# ── Core ──
fastapi>=0.100.0
uvicorn[standard]
python-dotenv

# ── Database ──
supabase>=2.0.0

# ── AI / LLM ──
ollama
google-genai>=0.3.0
groq>=0.8.0

# ── Knowledge Base (ChromaDB) ──
chromadb>=0.4.0
sentence-transformers>=2.2.0

# ── Speech ──
faster-whisper>=1.0.0
cartesia>=1.0.0

# ── Search ──
httpx
beautifulsoup4
lxml

# ── MCP ──
mcp>=1.0.0

# ── Media ──
pydub
soundfile
numpy

# ── LiveKit ──
livekit>=0.10.0
livekit-api>=0.10.0

# ── ML ──
torch
torchvision
torchaudio
accelerate

# ── UI Gen ──
Pillow

# ── Auth ──
passlib[bcrypt]
pyjwt